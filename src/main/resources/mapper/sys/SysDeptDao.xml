<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.sys.dao.SysDeptDao">

	<select id="queryObject" resultType="io.renren.modules.sys.entity.SysDeptEntity">
		select * from sys_dept where dept_id = #{value}
	</select>

	<select id="queryList" resultType="io.renren.modules.sys.entity.SysDeptEntity">
		select d.*,(select p.name from sys_dept p where p.dept_id = d.parent_id) as parentName
		from sys_dept d
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by d.${sidx} ${order}
            </when>
			<otherwise>
                order by d.order_num asc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from sys_dept 
	</select>
	 
	<insert id="save" parameterType="io.renren.modules.sys.entity.SysDeptEntity" useGeneratedKeys="true" keyProperty="deptId">
		insert into sys_dept
		(
			`parent_id`, 
			`name`, 
			`order_num`
		)
		values
		(
			#{parentId}, 
			#{name}, 
			#{orderNum}
		)
	</insert>
	 
	<update id="update" parameterType="io.renren.modules.sys.entity.SysDeptEntity">
		update sys_dept 
		<set>
			<if test="parentId != null">`parent_id` = #{parentId}, </if>
			<if test="name != null">`name` = #{name}, </if>
			<if test="orderNum != null">`order_num` = #{orderNum}</if>
		</set>
		where dept_id = #{deptId}
	</update>
	
	<delete id="delete">
		delete from sys_dept where dept_id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from sys_dept where dept_id in 
		<foreach item="deptId" collection="array" open="(" separator="," close=")">
			#{deptId}
		</foreach>
	</delete>

</mapper>